name: agent-framework
recipe: compose
services:
  app:
    type: compose
tooling:
  run:
    service: app
    description: Run the agent starter script
    cmd: python local_agent_framework.py
  shell:
    service: app
    description: Open a shell inside the app container
    cmd: /bin/bash
